# MediaServer v19.0 migration evidence

## Source decision

Debian 13 Trixie does not package the Jellyfin server. Its package search has
one name match, `python3-jellyfin-apiclient-python`, which is a client library.
The appliance therefore uses Jellyfin's official signed stable APT repository
for Trixie. The upstream index publishes and retains the compatible
`10.11.10+deb13` and `10.11.11+deb13` server, web, and metapackage builds.

The appliance verifies Jellyfin signing-key fingerprint
`4918 AABC 486C A052 358D 778D 4902 3CD0 1DE2 1A7B`, records the installed
version and source identity, and requires those values before its updater will
query or change packages. Version `10.11.11+deb13` corresponds to official tag
`v10.11.11`, commit `1fbd8739292cce610231be93daf43368733edf63`.

Evidence endpoints:

- Debian Trixie package search:
  `https://packages.debian.org/search?keywords=jellyfin&searchon=names&suite=trixie&section=all`
- Jellyfin Trixie package index:
  `https://repo.jellyfin.org/debian/dists/trixie/main/binary-amd64/Packages`
- Jellyfin stable Debian artifacts:
  `https://repo.jellyfin.org/files/server/debian/latest-stable/amd64/`

## README crosswalk

| README contract | Test command | Expected result | Actual result and retained evidence |
| --- | --- | --- | --- |
| Jellyfin administrator and web UI on 8096 | `tests/v19.sh` calls `System/Info/Public` and `Users/AuthenticateByName` with the firstboot password. | The public API returns the dpkg version, the bootstrap file is gone, and administrator login returns a token. | Pending the fresh exact run; the report and runtime result will be retained under the Wave 2 state directory. |
| Appliance TLS proxy on 12322 | `tests/v19.sh` calls `System/Info/Public` through the self-signed Apache endpoint. | Direct and proxied endpoints return the same Jellyfin version. | Pending the fresh exact run; the prior exact attempt stopped during build before runtime. |
| Music, Movies, TV Shows, and Photos libraries | `tests/v19.sh` reads virtual folders, generates and scans a one-second audio file, and requests playback metadata. | All four paths exist; the exact file is indexed with an audio stream and playable media-source identifier. | Pending the fresh exact run; the fixture is local and removed on exit. |
| Shared storage for Jellyfin and Samba | `tests/v19.sh` checks the `MEDIASERVER` Samba and WebDAV identities, `/srv/storage` share, `smbd`, Jellyfin's `users` membership, and the four Jellyfin library paths. | Both sides retain the common storage boundary and the appliance-specific service name. | Pending the fresh exact run. This replaces the stale README wording that called the shared paths a Jellyfin "path substitution." |
| Signed Jellyfin updates | `tests/updater-apply-fixture.sh` installs official `10.11.10+deb13`, invokes `turnkey-mediaserver-update --check` and `--apply`, and waits for 10.11.11. | A fresh administrator login, all libraries, the same item identity and media bytes, and all non-version source-record fields survive. | The official index-retention and static fixture gates pass; actual apply is pending the fresh exact run. |
| Jellyfin supervision | `tests/v19.sh` queries systemd before using the API; the update fixture restarts through the real helper and waits for the API. | Jellyfin is enabled and active, and returns after the package restart. | Pending the fresh exact run. |
| Hardware video acceleration prerequisites | `tests/v19.sh` verifies the packaged VA tool, Jellyfin's `video` group membership, and the media path with Jellyfin's packaged ffmpeg. | Software media discovery and the documented VA prerequisites work; host-specific acceleration is not claimed. | Software path pending exact; host GPU behavior is deferred below. |
| Optional direct Jellyfin HTTPS on 8920 | Follow the README's administrator-supplied PKCS #12 procedure. | Jellyfin can expose 8920 after a certificate is supplied. | Not run because no administrator certificate exists in the disposable fixture; the default appliance TLS proxy on 12322 is tested instead. |
| Samba users and shares, WebDAV CGI, Webmin Samba module, NFS, SFTP, rsync, and compression tools | Use the retained Fileserver v19 parent baseline; the focused test additionally checks MediaServer's changed name and storage boundary. | Unchanged parent services retain their v19 behavior and the MediaServer-specific substitutions are valid. | The wish records the Fileserver v19 main-flow baseline as satisfied; this candidate's narrow overlay assertions are pending exact. |
| SSH, system TLS, firstboot, backups, and other standard platform services | Reuse the Core v19 baseline because MediaServer does not replace those surfaces. | The inherited Core services retain their v19 behavior. | Core PASS report `20260824t010251z-1634-32241`; MediaServer's firstboot credential and TLS proxy are separately tested here. |

The focused test emits exactly the seven Wave 2 result keys after all runtime
and updater assertions succeed.

## Exact Wave 2 command

```sh
TKLDEV_CONTAINER=tkldev19-wave2 \
TKL_HARNESS_STATE_DIR=/home/agent/.local/state/turnkey-v19-harness-wave2 \
TKL_HARNESS_LOCK_FILE=/home/agent/.local/state/turnkey-v19-harness-wave2/build.lock \
TKL_HARNESS_LOCK_TIMEOUT=3600 \
TKL_HARNESS_DOCKER_LIMIT_BYTES=53687091200 \
/home/agent/turnkey/tools/test-v19-appliance mediaserver \
  --source /home/agent/.local/worktrees/turnkey-apps/mediaserver/wish-mediaserver-v19-trixie
```

## Candidate status and loop accounting

Exact run `20260826t115503z-2240736-11770` reproduced a main-flow startup
race: `/health` reported ready while the real `/Startup/Configuration` request
still returned HTTP 503. Product loop 1 retains the health gate and retries
only that startup request on transient failure for at most four minutes; a
terminal failure includes the final 100 Jellyfin log lines.

The focused 503 correction passed its bounded probe. The actual updater fixture
is validation-only and consumes no product loop. A fresh exact run of both is
pending on this candidate. Product-fix accounting remains 1 of 6.

## Deferred issues

- MEDIUM: Hardware-accelerated transcoding varies with host GPU, device
  passthrough, and Debian drivers. The exact runtime checks discovery and
  playback metadata without claiming host-specific acceleration.
- MEDIUM: Direct Jellyfin HTTPS on port 8920 requires an administrator-provided
  certificate. The appliance TLS proxy on port 12322 is tested instead.
- LOW: Administrator authentication and web serving use the real APIs but do
  not automate browser rendering.
